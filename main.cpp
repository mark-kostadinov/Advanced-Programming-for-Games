#include <iostream>

/// >> Number class (may be culled?): to encapsulate a lock number (should be 0 to 9), as well as
/// functions for turning a number (e.g. from 1 to 7 is either 2->6 or 0->8),
/// 
/// >> Lock class: (Number, Number, Number, Number), bool isPressed, function for pressing a button and one for unlocking
/// - LockTheLock() -> generate a ROOT integer and a CN as a result of hashing the ROOT
/// 
/// 
///
///
///
///
///
///
///
///
///
/// >> Safe class: constructor(int numOfLocks) -> add a new Lock each time to the class variable vector of Locks, one Lock by default
/// Start with a simple constructor for one lock, then 5 locks, then the above solution.

int main()
{
	return 0;
}